-module(recover_couchdb).

%% recover_couchdb: recover_couchdb library's entry point.

-export([main/1]).


%% API

main(_Opts) ->
    ok().

%% Internals

ok() ->
    io:format("Hello World!~n", []).

%% End of Module.
